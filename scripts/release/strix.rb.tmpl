# typed: false
# frozen_string_literal: true

# Generated by charliek/strix's release workflow (scripts/release/strix.rb.tmpl
# rendered by .github/workflows/release.yml). DO NOT EDIT.
class Strix < Formula
  desc "A focused, polished TUI for staging changes and viewing diffs"
  homepage "https://github.com/charliek/strix"
  version "@VERSION@"
  license "MIT"

  depends_on "git"

  on_macos do
    if Hardware::CPU.intel?
      url "https://github.com/charliek/strix/releases/download/@TAG@/strix_darwin_amd64.tar.gz"
      sha256 "@SHA_DARWIN_AMD64@"

      define_method(:install) do
        bin.install "strix"
      end
    end
    if Hardware::CPU.arm?
      url "https://github.com/charliek/strix/releases/download/@TAG@/strix_darwin_arm64.tar.gz"
      sha256 "@SHA_DARWIN_ARM64@"

      define_method(:install) do
        bin.install "strix"
      end
    end
  end

  on_linux do
    if Hardware::CPU.intel? && Hardware::CPU.is_64_bit?
      url "https://github.com/charliek/strix/releases/download/@TAG@/strix_linux_amd64.tar.gz"
      sha256 "@SHA_LINUX_AMD64@"

      define_method(:install) do
        bin.install "strix"
      end
    end
    if Hardware::CPU.arm? && Hardware::CPU.is_64_bit?
      url "https://github.com/charliek/strix/releases/download/@TAG@/strix_linux_arm64.tar.gz"
      sha256 "@SHA_LINUX_ARM64@"

      define_method(:install) do
        bin.install "strix"
      end
    end
  end

  test do
    system bin/"strix", "--version"
  end
end
