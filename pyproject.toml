[project]
name = "strix-docs"
version = "0.1.0"
description = "Documentation for the strix TUI"
requires-python = ">=3.12"

[dependency-groups]
docs = [
    "mkdocs>=1.6.1,<2.0",
    "mkdocs-material>=9.6",
    "pymdown-extensions>=10.15",
]
